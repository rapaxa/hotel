import UserProfile from "../pages/User/UserProfile";
const Header = () => {
    return (
        <header className='header'>
            <UserProfile />
        </header>
    );
};

export default Header;